# 切换到公开 PyPI（放弃私有源）

## 背景

之前 `zjcode` fork 走 `http://8.152.204.58:48080/simple/` 私有 `pypiserver`，`/update` 命令通过 `--index-url` 引导 `uv tool install -U` 从私有源升级。本轮决定放弃私有源，改为上传公开 PyPI（`https://pypi.org/`），并把版本从 `0.0.1` 重新开始。同时优先与上游 `deepagents-code` 保持同步，只保留 zjcode 品牌化必需的差异。

- pypi.org 上 `zjcode` 名字空的（JSON API 返回 404），可用。
- 判断包名可用性的标准接口只有 JSON API 和 Simple Index，`/project/<name>/` HTML 页会被 Cloudflare 反爬拦成 200，不能作为依据。

## 备份

工作区在切换前已经全部提交，HEAD 为 `b9afc1845`。

- 备份分支：`backup/learn-private-source-20260719-032811`
- 备份 tag：`backup/private-source-final-20260719-032811`

回滚方式：`git checkout backup/learn-private-source-20260719-032811`。

## 关键约束

- 分发名（PyPI project name）：`zjcode`
- Python 包目录：仍保留 `deepagents_code/`（降低上游合并冲突）
- CLI 命令：只保留 `zjcode`
- 状态目录：`~/.zjcode/`（与 `~/.deepagents/` 隔离）
- 环境变量前缀：`DEEPAGENTS_CODE_*` 保留

## 改动清单

### 版本重置

`python bump-version.py 0.0.1` 一把改齐三处：

- `libs/code/pyproject.toml` → `version = "0.0.1"`
- `libs/code/deepagents_code/_version.py` → `__version__ = "0.0.1"`
- `.release-please-manifest.json` → `"libs/code": "0.0.1"`

然后 `uv lock` 刷新 `uv.lock`。

### 生产代码

- `deepagents_code/_version.py`：重写为上游简洁版；`PYPI_URL = f"https://pypi.org/pypi/{DISTRIBUTION_NAME}/json"` 硬编码公开源；**删除 `PYPI_INDEX_URL` 常量、`os.environ.get()` 兜底**。仍保留 `DISTRIBUTION_NAME = "zjcode"`、`BRAND_NAME = "zjcode"`、`USER_AGENT = f"{DISTRIBUTION_NAME}/{__version__} update-check"`。
- `deepagents_code/_env_vars.py`：删除 `PYPI_INDEX_URL` 常量；保留 `PYPI_URL`（docstring 改成 zjcode）。
- `deepagents_code/update_check.py`：
  - 从 import 里去掉 `PYPI_INDEX_URL`
  - **完整删除 `_index_url_flag()` 函数**及其三处调用
  - `FALLBACK_UPGRADE_COMMAND` docstring 去掉 `--index-url` 段落
- `deepagents_code/config_manifest.py`：`NON_OPTION` 集合移除 `_env_vars.PYPI_INDEX_URL`。
- `bump-version.py`：docstring 改为 zjcode。

验证：`grep PYPI_INDEX_URL libs/code/` 和 `grep 8.152.204.58 libs/code/deepagents_code/` 均无匹配。

### 测试

- `tests/unit_tests/conftest.py`：删除 `_neutralize_private_pypi_index_url` autouse fixture（引用了已不存在的 `deepagents_code.update_check.PYPI_INDEX_URL`）。
- `tests/unit_tests/test_update_check.py`：删除 `TestUpgradeInstallCommand` 尾部整段私有源相关的四个用例（`test_appends_private_index_url_when_configured` / `test_appends_private_index_url_with_prerelease` / `test_shell_quotes_index_url_with_special_chars` / `test_omits_index_url_when_unset`）。

验证：`grep PYPI_INDEX_URL libs/code/tests/` 无匹配。

### 私有源专属产物删除

- `/Users/zhaojian/code/deepagents/web/` 整个目录（含 `install.sh` / `install.ps1` / `uninstall.sh` / `uninstall.ps1` / `index.html` / `pypi/`）
- `libs/code/run-build-upload.sh`
- `libs/code/doc/private_pypi_setup_guide.md`

### 开发脚本更新

- `libs/code/run-setup-dev-env.sh`：`dcode --help` → `zjcode --help`，`./run-build-upload.sh` → `./run-publish.sh`。
- `libs/code/setup-dev-env.ps1`：同上（`bash run-publish.sh`）。

### 新增 `libs/code/run-publish.sh`

极简发布脚本，走公开 PyPI：

```bash
#!/bin/bash
set -euo pipefail
cd "$(dirname "$0")"

rm -rf dist/
uv build
uv publish "$@"
```

依赖：`UV_PUBLISH_TOKEN` 或 `~/.pypirc` 提供凭据。Test PyPI 试跑：

```bash
UV_PUBLISH_TOKEN=<test-token> ./run-publish.sh --publish-url https://test.pypi.org/legacy/
```

## 有意保留的品牌化差异

- `pyproject.toml` `name = "zjcode"`、`[project.scripts] zjcode = ...`
- Python 包目录仍为 `deepagents_code/`
- `~/.zjcode/` 状态目录（生产代码内 8 处 `Path.home() / ".zjcode"`）
- shadow detection 单名 `(DISTRIBUTION_NAME,)`
- 环境变量前缀 `DEEPAGENTS_CODE_*`
- `USER_AGENT = "zjcode/..."`
- LangSmith telemetry 仍上报 `deepagents-code`（有意保留兼容性）

## 测试结果

`uv run --group test pytest tests/unit_tests/ -q --ignore=tests/unit_tests/tui/widgets/test_messages.py --ignore=tests/unit_tests/test_app.py` → **7712 passed, 3 skipped**。

两处跳过测试：

- `tui/widgets/test_messages.py`：上游既有 flaky（stash 之前也复现，与品牌化无关）。
- `test_app.py`：`TestGoalCommand::test_initial_goal_acceptance_submits_objective` teardown 时 OpenAI APIConnectionError（网络泄漏 flaky），非本轮改动引入。

## 上传流程

1. 先 Test PyPI 演习：
   ```bash
   UV_PUBLISH_TOKEN=<test-token> ./run-publish.sh --publish-url https://test.pypi.org/legacy/
   ```
2. 确认无误后正式发：
   ```bash
   UV_PUBLISH_TOKEN=<pypi-token> ./run-publish.sh
   ```
3. token **不要贴到聊天里**，用 shell 环境变量或 `~/.pypirc`。

## 老用户迁移路径

如果本机之前装的是私有源版本：

```bash
uv tool uninstall zjcode  # 或旧的 deepagents-code
uv tool install zjcode    # 默认公开 PyPI
```

`/update` 命令走公开 PyPI，无需 `--index-url`。
> ⏹ 结束状态：✅ 正常结束 (finish_reason=stop) | ⏱ 总耗时：59m 53s | 🕒 结束时间：2026-07-19 04:38:31
