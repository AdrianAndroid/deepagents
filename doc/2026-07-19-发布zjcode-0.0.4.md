# 2026-07-19 发布 zjcode 0.0.4

## 用户请求
"发布版本0.0.4"

按照 AGENTS.md 的默认发布目标规则和 `publish-zjcode-version` skill，默认目标为 `libs/code/`（分发名 `zjcode`），发布到公开 PyPI。

## 执行步骤

### 1. 预检
- `git status --short`：干净
- 当前分支：`learn` ✅
- `git tag -l "zjcode-v0.0.4"`：不存在 ✅
- `https://pypi.org/pypi/zjcode/0.0.4/json`：HTTP 404 ✅
- 当前 pyproject 版本：`0.0.3`
- `deepagents==0.7.0a7` pin：存在
- workflow 的 "Relax deepagents pin" 补丁：`PATCH_ALREADY_PRESENT` ✅ 无需再改 workflow

### 2. 版本 bump
```bash
python libs/code/bump-version.py 0.0.4
```
三处同步更新：
- `libs/code/pyproject.toml`：0.0.3 → 0.0.4
- `libs/code/deepagents_code/_version.py`：0.0.3 → 0.0.4
- `.release-please-manifest.json`（`libs/code`）：0.0.3 → 0.0.4

### 3. 刷新 lockfile
`uv lock` → `Updated zjcode v0.0.3 -> v0.0.4`

### 4. 本地构建验证
```
Successfully built dist/zjcode-0.0.4.tar.gz
Successfully built dist/zjcode-0.0.4-py3-none-any.whl
```

### 5. 提交与推送
- commit：`chore(code): bump zjcode to 0.0.4`（4 files changed）
- push origin learn → 成功

### 6. 打标签触发发布
```bash
git tag zjcode-v0.0.4
git push origin zjcode-v0.0.4
```
标签已推送，触发 `.github/workflows/publish-zjcode.yml`（OIDC Trusted Publisher，无需 token）。

## 验证入口
- Actions：https://github.com/AdrianAndroid/deepagents/actions/workflows/publish-zjcode.yml
- PyPI：https://pypi.org/project/zjcode/0.0.4/
- 校验 metadata 中 `deepagents` requirement 应为 `>=0.7.0a7,<0.8.0`（CI sed 步骤自动松绑，libs/code/pyproject.toml 本身保留 `==0.7.0a7` 与 upstream 一致）
- 冒烟：`uv tool install zjcode` 无需 `--prerelease=allow` 即可安装

## 备注
- 未执行 dry-run（用户未要求）
- 未修改 upstream release-please 工作流
- 未使用 `git add -A`，只暂存了 4 个明确文件
