# 手动保存当前会话验证自动保存功能

## 会话内容
1. **用户提问**：等读取这个git项目的skill吗,每次问答文档能保存到项目doc目录下吗?
2. **回答**：可以的，两个需求都已经支持：
   - 项目Skill读取：当前项目内置了`remember`（记忆功能）和`skill-creator`（自定义skill创建）两个通用Skill，可直接使用
   - 问答文档自动保存：已经正常运行，doc目录下已有大量历史问答记录，按「年-月-日-时分秒-标题.md」格式自动保存
3. **用户提问**：这次会话怎么没有保存到本地?
4. **回答**：会话还在进行中，默认是会话结束时（/exit退出或会话断开）才会自动生成文件保存。如果需要立即保存，可以执行`/remember`命令。
5. **用户操作**：执行手动保存

## 保存时间
2026-07-01 15:11:00
